8803_project2
Michelle Scott
=============

There are 3 folders: client, proxy, and server.
Each folder has a makefile. 

The executables have the same name as the folder. Adding the -u flag for a call prints the usage.

The proxy signal that deletes existing shared memory is SIGQUIT.
